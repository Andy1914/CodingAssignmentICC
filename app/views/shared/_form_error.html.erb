<% if model_data.errors.any? %>
  <div style="color: red">
    <h2><%= pluralize(model_data.errors.count, "error") %> prohibited this storefront from being saved:</h2>
    <ul>
      <% model_data.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>